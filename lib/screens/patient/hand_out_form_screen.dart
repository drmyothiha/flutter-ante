import 'package:flutter/material.dart';

class HandOutFormScreen extends StatelessWidget {
  const HandOutFormScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Hand Out Form')),
      body: const Center(child: Text('Hand Out Form (demo)')),
    );
  }
}
