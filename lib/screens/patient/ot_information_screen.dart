import 'package:flutter/material.dart';

class OtInformationScreen extends StatelessWidget {
  const OtInformationScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('OT Information')),
      body: const Center(child: Text('OT Information (demo)')),
    );
  }
}
