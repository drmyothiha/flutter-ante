import 'package:flutter/material.dart';

class NotesContent extends StatelessWidget {
  const NotesContent({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Surgical Notes (demo)'));
  }
}
