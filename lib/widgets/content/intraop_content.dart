import 'package:flutter/material.dart';

class IntraopContent extends StatelessWidget {
  const IntraopContent({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Intraoperative Management (demo)'));
  }
}
